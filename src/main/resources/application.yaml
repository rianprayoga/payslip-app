#logging:
#  level:
#    root: debug

spring:
  application:
      name: payslip-service
  datasource:
    url: jdbc:postgresql://localhost:5432/payslips
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
